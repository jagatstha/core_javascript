/** Array.prototype.shift()
 * The shift() method removes the first element from an array and returns that 
 * removed element. This method changes the length of the array.
 * shift is intentionally generic; this method can be called or
 *  applied to objects resembling arrays.
 * 
 * syntax: arr.shift()
 * return:  the removed element from array. undefined if array is empty
 */

 var myFish = ['angel', 'clown', 'mandarin']
console.log(myFish.shift())